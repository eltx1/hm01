name: Task18 Diagnostic

'on':
  push:
    branches:
      - task-18-direct-demand-admin-control-center

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: task-18-direct-demand-admin-control-center
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, sqlite3, pdo_sqlite, soap
      - run: composer install --prefer-dist --no-interaction --no-progress
      - name: Capture first failure
        shell: bash
        run: |
          cp .env.example .env
          php artisan key:generate
          set +e
          php artisan test --stop-on-failure 2>&1 | tee task18-first-failure.log
          echo "test_exit=${PIPESTATUS[0]}" >> "$GITHUB_ENV"
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: task18-first-failure
          path: task18-first-failure.log
          retention-days: 1
